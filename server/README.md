# Backend Online Quiz

Backend Express, JWT và SQLite cho hệ thống bài thi trắc nghiệm.

```powershell
npm install
npm run seed
npm start
```

Server chạy tại `http://localhost:3000`, API base URL là `http://localhost:3000/api`.
