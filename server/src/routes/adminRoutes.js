const r=require("express").Router(),c=require("../controllers/adminController"),auth=require("../middleware/authMiddleware"),admin=require("../middleware/adminMiddleware");r.use(auth,admin);r.get("/stats",c.stats);r.get("/quizzes",c.quizzes);r.post("/quizzes",c.createQuiz);r.put("/quizzes/:id",c.updateQuiz);r.delete("/quizzes/:id",c.deleteQuiz);r.get("/quizzes/:id/questions",c.questions);r.post("/quizzes/:id/questions",c.createQuestion);r.put("/questions/:id",c.updateQuestion);r.delete("/questions/:id",c.deleteQuestion);r.get("/comments",c.comments);r.delete("/comments/:id",c.deleteComment);r.get("/contacts",c.contacts);module.exports=r;
